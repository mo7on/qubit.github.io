// This script can be run manually to initialize the scheduler
import { initializeScheduler } from '../lib/scheduler';

console.log('Manually initializing article generation scheduler...');
initializeScheduler();
console.log('Scheduler initialization complete.');